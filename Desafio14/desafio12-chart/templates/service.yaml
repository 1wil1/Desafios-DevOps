apiVersion: v1
kind: Service
metadata:
  name: {{ include "desafio12-chart.fullname" . }}
  labels:
    {{- include "desafio12-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "desafio12-chart.selectorLabels" . | nindent 4 }}
